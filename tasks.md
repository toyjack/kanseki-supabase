# 実装タスク

## フェーズ1: 開発基盤

- [x] `/specs` 配下の仕様を確認する
- [x] Bunの利用方針と開発基盤の受け入れ基準を仕様へ追加する
- [x] Bun + TypeScriptプロジェクトを初期化する
- [x] `bun test`、`bun run lint`、`bun run typecheck`を構成する
- [x] 日文READMEに開発・テスト・運用コマンドを記載する

### 受け入れ基準

- `bun install`で依存関係を再現できる
- `bun test`、`bun run lint`、`bun run typecheck`が成功する
- `bun.lock`以外のパッケージマネージャー用ロックファイルが存在しない

## フェーズ2: データベース

- [x] Supabase/PostgreSQL向けの接続構成と`.env.example`を作成する
- [x] PGroongaを有効化するマイグレーションを作成する
- [x] 中核層・典拠層・検索支援層のテーブルを作成する
- [x] 外部キー、複合自然キー、自己参照防止制約を追加する
- [x] 異体字テーブルと検索対象カラムのPGroonga索引を作成する
- [x] マイグレーションの契約テストを作成する
- [x] PostgreSQL上でロールバックされる結合テストを作成する

### 受け入れ基準

- 仕様4章の全テーブル、カラム、関連、制約がマイグレーションに反映される
- `(fa_code, collection_id, nu)`のスコープでレコードを一意に扱える
- `oy_record_id = id`がデータベース制約で拒否される
- 検索対象に異体字正規化付きPGroonga索引が定義される

## フェーズ3: ETLコア

- [x] 16進コードポイント表記のUTF-8デコーダーを実装する
- [x] 標準XML類似タグ形式の`.dat`パーサーを実装する
- [x] AUTHORSの分解を実装する
- [x] ピンインと`ti_key`の正規化を実装する
- [x] レコード・著者の変換を実装する
- [x] 機関・コレクションの変換を実装する
- [x] 入力順に依存しない`oy_record_id`解決を実装する
- [x] 2パスのデータベース投入を実装する
- [x] 自己参照・循環参照の検出と警告を実装する
- [x] `WORKS`の仮グルーピングを実装する
- [x] 機関単位のトランザクションと失敗分離を実装する
- [x] 論理削除を実装する
- [ ] 各受け入れ基準を網羅する単体・結合テストを作成する

### 受け入れ基準

- 4桁および先頭が`2`の5桁コードポイントを正しく復号できる
- FA011962の独立形式を処理対象に含めない
- ファイル順序に依存せず親子関係を解決できる
- 自己参照と循環参照を保存せず、他機関の処理を継続できる
- 削除されたソースは物理削除せず`deleted_at`へ反映される

## フェーズ4: 同期・検索・運用

- [x] `rsync --itemize-changes`の結果から`.dat`差分対象を抽出する
- [x] シェルを介さないrsync実行処理を実装する
- [ ] rsyncソースとの接続を検証する
- [x] 機関・コレクション対応付けを実装する
- [x] UniVariantsとOpenCC単字データの統合処理を実装する
- [ ] UniVariantsとOpenCCの入力ファイルパーサーを実装する
- [x] 同一文字の競合時にUniVariantsを優先する
- [ ] 日次実行、ログ、取り込み状態記録を構成する
- [x] self-hosted SupabaseでPGroongaの作成・異体字検索・ピンイン検索を検証する
- [ ] 全体READMEと運用手順を完成させる

### 受け入れ基準

- 転送された変更だけをDB処理対象にできる
- 異体字マッピングが片方向・多対1で生成される
- 最新取り込み日時と機関ごとの成否を記録できる
- self-hosted環境でPGroongaの作成と検索結果を確認できる

## 保留事項

- 運用ダッシュボードの配置決定と実装
- バックアップ・リストア方針の設計
- 198万件環境でのREINDEX所要時間計測と更新承認フロー
- FA011962、横断検索、手作業による典拠名寄せ
